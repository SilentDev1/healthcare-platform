// Backward-compatible alias. /providers is the canonical consumer directory route;
// /facilities keeps working and renders the same provider-neutral directory.
export { default, metadata } from "../providers/page";
